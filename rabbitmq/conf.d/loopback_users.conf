# Docker forwards host connections through the bridge network, so RabbitMQ sees them
# as non-loopback traffic. The default "guest" user is restricted to loopback-only
# connections; disable that restriction so guest/guest works from host apps the same
# way it does for every other dev-only service in this compose file.
loopback_users.guest = false
